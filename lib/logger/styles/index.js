const marquee = require('./marquee');
const plain = require('./plain');
const statement = require('./statement');
const warning = require('./warning');

module.exports = { marquee, plain, statement, warning };
